package com.holly.analyzer.elasticsearch;

public class ElasticSearchTools {/*
	//客户端实例
	static Client client;
	
	static{
		try {
		    InputStream is = ElasticSearchTools.class.getClassLoader().
		    		getResourceAsStream("elastic-cluster.properties");
		    InputStreamStreamInput isi = new InputStreamStreamInput(is);
			Settings settings =  Settings.readSettingsFromStream(isi);
			
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}
*/}
